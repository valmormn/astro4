---
export interface Props {
	title: string;
	body: string;
	href: string;
}

const { href, title, body } = Astro.props;
---

<button class="btn btn-primary">
	<slot/>
</button>

<!-- /a styled daisy-ui button implemented in astro file -->

<!-- <button class="btn btn-primary"></button> -->
